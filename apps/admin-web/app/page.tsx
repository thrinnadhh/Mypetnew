export default function AdminHome() {
  return (
    <section className="card" aria-labelledby="admin-heading">
      <h2 id="admin-heading">Sprint 1 control plane shell</h2>
      <p className="muted">Sign-in, canonical ADMIN role, scoped permission, reason, idempotency key, step-up authentication, and audit context are required before any sensitive command is enabled.</p>
    </section>
  )
}

