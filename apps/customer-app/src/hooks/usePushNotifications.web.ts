export function usePushNotifications(
  userId?: string | null,
  accessToken?: string | null,
) {
  // Push-notification registration and response handling are native-only.
  void userId;
  void accessToken;
}
